[pytest]
markers =
    live: tests that drive the real game (require Tyranny + bridge mod; skipped in CI)
testpaths = tests
norecursedirs = puffer_fork tools pipeline/out snapshots corpora
addopts = -m "not live"
